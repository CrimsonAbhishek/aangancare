# Visual QA notes

Desktop screenshots were captured for `/`, `/request`, and `/admin` at 1280×720. The public home has a strong first viewport: calm ivory background, clear serif headline, a single dark request CTA, and the floral visual anchored on the right. The request flow reads clearly as a three-step form with the first decision visible without scrolling. The operations preview has a distinct dark sidebar, clear stats, a request queue, and quick actions; it is visually separate from the public experience as intended.

The hero image is intentionally soft and has a visible stock watermark because it is a temporary visual research asset. Replace it with licensed or owned imagery before production launch. Business phone, email, location coverage, and pricing remain explicit placeholders by design.
